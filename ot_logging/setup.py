from setuptools import setup

setup(
    name='ot_logging',
    version='0.2',
    py_modules=['ot_logging'],
    install_requires=[
        # list any required packages here
    ],
)

# V0.1: Initial development up until the point production level processes could be implemented
# V0.2: First official release; production level processes can comfortably utilize.