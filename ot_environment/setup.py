from setuptools import setup

setup(
    name='ot_environment',
    version='0.1.1',
    py_modules=['ot_environment'],
    install_requires=[
        # list any required packages here
    ],
)